import { Box } from "@chakra-ui/react";

import MenuForm from "../components/order/MenuForm";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <MenuForm />
    </Box>
  );
};

export default Home;
