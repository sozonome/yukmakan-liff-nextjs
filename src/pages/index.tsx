import { Box } from "@chakra-ui/react";

import MenuFormContainer from "../components/order/MenuFormContainer";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <MenuFormContainer />
    </Box>
  );
};

export default Home;
